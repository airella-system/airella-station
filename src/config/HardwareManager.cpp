#include "config/HardwareManager.h"
#include "maintenance/Logger.h"

WeatherSensorConfig HardwareManager::getWeatherSensorConfig() {
  WeatherSensorConfig config;
  return config;
}

AirSensorConfig HardwareManager::getAirSensorConfig() {
  AirSensorConfig config;
  return config;
}

PowerSensorConfig HardwareManager::getPowerSensorConfig() {
  PowerSensorConfig config;
  return config;
}

HeaterConfig HardwareManager::getHeaterConfig() {
  HeaterConfig config;
  return config;
}

StorageConfig HardwareManager::getStorageConfig() {
  StorageConfig config;
  return config;
}
